---
import katex from 'katex';
import '../styles/animated-tex.css';

interface Props {
    tex: string;
}

const { tex } = Astro.props;
---

<Fragment set:html={katex.renderToString(tex, { trust: true })} />

<script src="../../public/client-animated-tex.ts"></script>
